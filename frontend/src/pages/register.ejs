<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>H(T)ML Website</title>
    <link rel="stylesheet" href="/styles/login.css">
</head>

<body>
    <!-- Header Section -->
    <%- include('../partials/header', { activeIdx: 0 }) %>

    <% if (message != null ) { %> <script> alert('<%= message %>'); </script> <% } %>
    <!-- Main Content Section -->
    <main class="main-content">
        <div class="login-container">
            <img src="/images/Login-Register-Picture.png" alt="Banner Image" class="login-banner">
            <div class="login-form">
                <h2>Đăng Ký</h2>
                <form action="/register" method="POST"> <!-- Change action to /register -->
                    <div class="input-group">
                        <input type="text" name="name" placeholder="Tên Người Dùng" required> <!-- Username field -->
                    </div>
                    <div class="input-group">
                        <input type="email" name="email" placeholder="Email" required> <!-- Email field -->
                        <small id="email-error" style="color: red;"></small>
                    </div>
                    <div class="input-group">
                        <input type="password" id="password" name="password" placeholder="Mật Khẩu" required>
                        <img src="/icons/icon-hiddeneyes.svg" alt="Toggle Visibility" class="visibility-icon" onclick="togglePasswordVisibility()">
                    </div>
                    <div class="input-group">
                        <input 
                            type="tel" 
                            inputmode="numeric" 
                            pattern="[0-9]*" 
                            name="phone" 
                            placeholder="Số Điện Thoại" 
                            required 
                            onkeypress="return event.charCode >= 48 && event.charCode <= 57"
                        >
                    </div>
                    <input type="hidden" name="role" value="Customer">
                    <button type="submit" class="login-button">Đăng Ký</button> 
                    
                </form>
                <p>Đã Có Tài Khoản? <a href="/login">Đăng Nhập Ngay!</a></p> <!-- Update link to login -->
                <p class="terms">
                    Bằng Việc Nhấn “Đăng Ký”, Bạn Đồng Ý Với 
                    <a href="/terms-and-conditions">Điều Khoản & Điều Kiện</a> Và 
                    <a href="/terms">Chính Sách Quyền Riêng Tư</a> Của Chúng Tôi.
                </p>
            </div>
        </div>
    </main>

    <!-- Footer Section -->
    <%- include('../partials/footer', { activeIdx: 0 }) %>
<script>
        // Define a function to toggle the password visibility
        function togglePasswordVisibility() {
            const passwordInput = document.getElementById('password');
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
            } else {
                passwordInput.type = 'password';
            }
        }
</script>   
</body>
</html>
